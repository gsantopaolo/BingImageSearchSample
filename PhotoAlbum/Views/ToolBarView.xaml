<UserControl
    x:Class="PhotoAlbum.Views.ToolBarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PhotoAlbum.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:vm="using:PhotoAlbum.ViewModels"
    xmlns:Behaviors="using:Template10.Behaviors"
      xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
      xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <!--<UserControl.DataContext>
        <vm:ToolBarViewModel/>
    </UserControl.DataContext>-->
    <StackPanel Width="300" Height="80" Orientation="Horizontal" Background="Red">

        <TextBox Text="{Binding Query, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="10"/>
        <Button Content="search" Margin="10" HorizontalAlignment="Right" Click="ToolBarView_Loaded" Command="{Binding QueryCommand, Mode=OneWay}">

        </Button>

    </StackPanel>
</UserControl>
